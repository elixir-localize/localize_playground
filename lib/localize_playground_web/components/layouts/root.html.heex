<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={Phoenix.Controller.get_csrf_token()} />
    <title>Localize Playground</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link phx-track-static rel="stylesheet" href="/assets/app.css" />
    <%!-- MF2 editor scripts must appear BEFORE app.js so that by
          the time app.js's `defer` callback runs and constructs the
          LiveSocket, `window.Mf2WasmEditor.Hooks` is populated.
          All three scripts use `defer`, which executes in document
          order after parsing — so ordering here is what wires the
          hook into LiveSocket. --%>
    {raw(Mf2WasmEditor.script_tags())}
    <script defer phx-track-static type="text/javascript" src="/assets/app.js">
    </script>
  </head>
  <body>
    {@inner_content}
  </body>
</html>
